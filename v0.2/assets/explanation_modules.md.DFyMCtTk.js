import{_ as t,o,c as a,ao as s}from"./chunks/framework.b9pX6m-X.js";const m=JSON.parse('{"title":"Modules, vocabularies and glue","description":"","frontmatter":{},"headers":[],"relativePath":"explanation/modules.md","filePath":"explanation/modules.md","lastUpdated":null}'),d={name:"explanation/modules.md"};function r(n,e,i,l,c,h){return o(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="Modules,-vocabularies-and-glue" tabindex="-1">Modules, vocabularies and glue <a class="header-anchor" href="#Modules,-vocabularies-and-glue" aria-label="Permalink to &quot;Modules, vocabularies and glue {#Modules,-vocabularies-and-glue}&quot;">​</a></h1><p>Three things in this system are easy to confuse. Each lives in a different place and has a different lifetime.</p><ul><li><p>A <strong>module</strong> is JavaScript. It renders. It runs in the browser.</p></li><li><p>A <strong>payload vocabulary</strong> is Julia types. The payloads a module reads are built from them. Each vendored module&#39;s vocabulary lives inside CesiumLink, so that every caller can reach it. A module you ship yourself carries its own.</p></li><li><p>A <strong>glue package</strong> is a Julia package. It turns a simulation&#39;s own types into the messages a module understands.</p></li></ul><h2 id="A-module" tabindex="-1">A module <a class="header-anchor" href="#A-module" aria-label="Permalink to &quot;A module {#A-module}&quot;">​</a></h2><p>One ES module, served same-origin, whose default export has a <code>setup(ctx)</code>. It renders or contributes to the scene, and it is the only place browser-side code is written. The Core hands <code>setup</code> one context object holding every capability a module may use. Every registration returns a disposable, and the Core drains them on unload, so a module cannot leave a listener behind in a shared service.</p><p>Most scenes need no module at all. Four modules are vendored: <code>primitives</code> draws points, polylines and ground footprints from payloads built in Julia, <code>ui</code> owns the overlay and the tooltip, <code>heatmap</code> drapes a grid of finished colour over a box of degrees, and <code>models</code> draws one glTF model per entity of a node family. A module is worth writing at the edge of those: a shader-driven material, a raster field, a widget kind of your own. The <a href="./../reference/wire/module-api">module API</a> is the normative contract, and <a href="./../tutorials/first-module">Write a viewer module</a> is the walk-through.</p><p>A module may also draw <strong>onto</strong> what another module owns. A sensor cone stands on an entity <code>primitives</code> owns: the drawer asks that module where the entity is and whether it is shown, every frame, and borrows its identity, so a click on the cone answers the satellite. <code>lib/models/</code> is the example to read. A borrowed identity costs what ADR-0023 states: a pick says which entity was hit, and never which geometry.</p><h2 id="What-is-vendored,-and-what-is-a-module-away" tabindex="-1">What is vendored, and what is a module away <a class="header-anchor" href="#What-is-vendored,-and-what-is-a-module-away" aria-label="Permalink to &quot;What is vendored, and what is a module away {#What-is-vendored,-and-what-is-a-module-away}&quot;">​</a></h2><p>The four vendored modules were chosen from satellite scenes. Nothing in the wire or in the Core knows what a satellite is; the vocabulary above them is points, links, footprints and a draped colour grid because that is what a constellation is made of. What a satellite scene needs is vendored, and the rest of what Cesium draws is reachable but not shipped.</p><p>Reachable, because a module is handed <code>ctx.Cesium</code> — the whole <code>@cesium/engine</code> namespace, the same instance the Core built the scene with — together with the scene, the widget&#39;s entity collection and its data sources. Each row below is a module nobody has written yet.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Cesium feature</th><th style="text-align:right;">Vendored?</th><th style="text-align:right;">What it takes today</th></tr></thead><tbody><tr><td style="text-align:right;"><strong>3D Tiles</strong> — photogrammetry, point clouds, OSM Buildings</td><td style="text-align:right;">no</td><td style="text-align:right;">A module: <code>Cesium3DTileset.fromUrl</code> onto <code>ctx.scene.primitives</code>. Serve the tileset from an <code>assets</code> mount, or name its origin in <code>trusted_origins</code></td></tr><tr><td style="text-align:right;"><strong>Terrain</strong> (quantized-mesh providers)</td><td style="text-align:right;">no</td><td style="text-align:right;">A module can set <code>scene.globe.terrainProvider</code>, but the globe is ellipsoid terrain by declaration and <code>Areas</code> and <code>heatmap</code> are authored on the ellipsoid, so ground geometry does not clamp to it. Session-wide terrain wants a declaration knob beside <code>imagery</code></td></tr><tr><td style="text-align:right;"><strong>Extruded polygons, walls, corridors, cylinders and cones</strong> — sensor volumes, swaths</td><td style="text-align:right;">no: <code>primitives</code> draws points, polylines and flat ground footprints</td><td style="text-align:right;">A module, which may stand its geometry on an entity <code>primitives</code> owns and borrow its identity, as <code>models</code> does</td></tr><tr><td style="text-align:right;"><strong>CZML</strong></td><td style="text-align:right;">no</td><td style="text-align:right;">A module: <code>CzmlDataSource.load</code> into <code>ctx.viewer.dataSources</code>. It rides beside the window buffer rather than through it, because CZML carries its own clock, availability and document range, and the Core owns the clock and the declared range</td></tr><tr><td style="text-align:right;"><strong>GeoJSON / KML / KMZ</strong></td><td style="text-align:right;">no</td><td style="text-align:right;">A module, the same way, and with no clock to argue about for a static overlay</td></tr><tr><td style="text-align:right;"><strong>Cesium ion assets</strong> — World Terrain, Bing imagery, Google Photorealistic 3D Tiles</td><td style="text-align:right;">no, deliberately: the viewer carries no token and works offline</td><td style="text-align:right;">A module that sets <code>Ion.defaultAccessToken</code> to your own token, and the ion origins in <code>trusted_origins</code></td></tr><tr><td style="text-align:right;"><strong>Imagery providers</strong> past a <code>{z}/{x}/{y}</code> template or a folder of tiles — WMS, WMTS, TMS, ArcGIS</td><td style="text-align:right;">no</td><td style="text-align:right;">A module that pushes an <code>ImageryProvider</code> onto <code>scene.imageryLayers</code>, or a widened <code>Imagery</code> declaration</td></tr><tr><td style="text-align:right;"><strong>Post-processing</strong> — silhouettes, bloom, custom shaders</td><td style="text-align:right;">no</td><td style="text-align:right;">A module: <code>ctx.scene.postProcessStages</code></td></tr></tbody></table><p>None of that needs a change to the Core, which owns four things a module cannot take over: the clock and the declared range, the window buffer and the requests that fill it, pick dispatch, and overlay placement. Work lands <em>in</em> the Core when it wants to own time or delivery: streaming CZML as a second delivery path, a scene with two clocks, or terrain and an external tileset as part of the session declaration.</p><h2 id="A-payload-vocabulary" tabindex="-1">A payload vocabulary <a class="header-anchor" href="#A-payload-vocabulary" aria-label="Permalink to &quot;A payload vocabulary {#A-payload-vocabulary}&quot;">​</a></h2><p><code>Nodes</code>, <code>Edges</code> and <code>Areas</code> are the vocabulary of <code>primitives</code>. The controls, the floats and the tooltip are the vocabulary of <code>ui</code>. <code>Models</code> is the vocabulary of <code>models</code>. <code>Raster</code> and the colour grid are the vocabulary of <code>heatmap</code>.</p><p>The vendored modules&#39; vocabularies live inside CesiumLink rather than in a package of their own (ADR-0011). Roughly a thousand lines of the package are the vocabulary of the vendored modules, and a split was declined on two grounds. The consumer count was too low for the seam to be real. And a split would turn the module API version into a compatibility axis, when that version is forwarded to the viewer and checked nowhere on the Julia side: a mismatch surfaces as one console line in the browser, a process away from the <code>[compat]</code> bound that would have caught it.</p><p>Each vendored module&#39;s vocabulary is a submodule — <code>CesiumLink.Primitives</code>, <code>CesiumLink.UI</code>, <code>CesiumLink.Heatmap</code> — and the file, the submodule and the wire id agree three ways (ADR-0012). A reader then sees what a 500-line vocabulary file needs from the rest of the package before reading any of it.</p><p>A module that ships from a package of your own carries its vocabulary in that package, beside the module and the entry that declares it. See <a href="./../tutorials/package-with-module">Ship a module from a Julia package</a>.</p><p><code>models</code> is the one exception, forced by a language rule: a module and a type inside it cannot share a name, because a <code>module Models</code> holding a <code>struct Models</code> makes the module binding win in the parent and leaves the constructor unreachable. Its submodule is <code>CesiumLink.ModelFamilies</code>, the file and the wire id still read <code>models</code>, and <code>using CesiumLink</code> supplies <code>Models</code> as it supplies <code>Nodes</code>.</p><p>The submodules <strong>are</strong> re-exported, so <code>using CesiumLink</code> alone supplies <code>Nodes</code> and <code>Title</code>. A refusal to re-export makes the compiler enforce the boundary, because Julia resolves a function body&#39;s names at call time. But it guards a rule that is almost never broken, and charges one import line to every consuming scope: roughly 37 test blocks in the package, 16 more in the end-to-end suite, two tool scripts and the extension. Reading and one grep check the boundary instead, and a vocabulary-to-vocabulary edge still appears only as a <code>using ..Primitives</code> line in a header.</p><p>The colour map and the geodesy helpers stay outside both vocabularies, because both vocabularies and scenes reach them directly and either one inside a vocabulary would create the single edge the boundary prevents. A submodule is earned by a vendored browser module, and neither has one.</p><h2 id="A-glue-package" tabindex="-1">A glue package <a class="header-anchor" href="#A-glue-package" aria-label="Permalink to &quot;A glue package {#A-glue-package}&quot;">​</a></h2><p>The Julia package that authors a module&#39;s messages, by turning a simulation&#39;s own types into the payloads a module reads. A package that draws its own constellation through the vendored <code>primitives</code> module is one, and a glue package that targets only vendored modules ships no JavaScript, no module folder and no build step.</p><p>Take <code>Nodes(...)</code>. The call is the vocabulary, and the glue is the function that knows a constellation has satellites and turns them into that call. Every glue package that targets <code>primitives</code> makes the same call, so <code>Nodes</code> sits in CesiumLink. Each glue package knows one domain, so the glue stays in the package that owns that domain.</p><p>A glue package that ships a module of its own holds that module&#39;s vocabulary too. What lives in CesiumLink is the vocabulary of the modules CesiumLink vendors.</p><h2 id="What-may-cross-between-two-modules" tabindex="-1">What may cross between two modules <a class="header-anchor" href="#What-may-cross-between-two-modules" aria-label="Permalink to &quot;What may cross between two modules {#What-may-cross-between-two-modules}&quot;">​</a></h2><p>Modules that derive from the same simulation base share their <strong>authoring code</strong>, not their <strong>runtime state</strong> (ADR-0006). Two heatmaps over one constellation share the Julia extraction that produces their fields and stream separate payloads into one vendored module.</p><p>The browser-side mechanism is narrow. <code>ctx.modules.get(id)</code> returns another declared module&#39;s <strong>exports</strong> — classes, factories, functions — and nothing else, so what crosses a module boundary is code by construction. A module that owns entities and wants them anchorable exports the anchor surface: <code>positionOf</code>, <code>countOf</code>, <code>pickIdOf</code>, <code>showOf</code>, <code>edgeEndpoints</code> and <code>pairsOf</code>. <code>ui</code> calls <code>positionOf</code> on the module a float&#39;s anchor names, and <code>models</code> calls four of the six on <code>primitives</code> every frame.</p><p>The rule is: <strong>one owner per entity; other modules get read-only accessors; writes never cross a module boundary.</strong> A module may draw over what another draws, and may never restyle or mutate it: a viewer-side mutation has no author on the server, so the next window silently overwrites it, and the bug is invisible until a window arrives.</p><h2 id="Declared-over-the-wire" tabindex="-1">Declared over the wire <a class="header-anchor" href="#Declared-over-the-wire" aria-label="Permalink to &quot;Declared over the wire {#Declared-over-the-wire}&quot;">​</a></h2><p>The server tells the viewer which modules to load. A declaration carries an id, a URL and an API version, and travels as part of establishing the connection (ADR-0009).</p><p>A declaration is authored in Julia by naming a <strong>file on disk</strong>. The server mounts that file&#39;s directory under a URL segment derived from the module id, and serves it same-origin, so sibling chunks, workers and images resolve normally and any Julia package ships a bundle by naming its own module folder.</p><p>The design this replaced was a static manifest file per module folder, discovered from a server-provided list. It made a folder a self-contained drop-in unit, but the unit that travels is a <strong>Julia package</strong>, which already has a name, a version and a place to put assets. The manifest duplicated all three into a file the Core had to fetch and validate first, and it cost two round trips at startup, for a version gate that is one field in a message the connection already sends.</p><p>The <code>apiVersion</code> gate survives, and it is checked <strong>before</strong> the import, so a module written against a different contract never has its code run. A mismatch is warned about and skipped, and the rest of the declaration still loads.</p><h2 id="What-declaration-order-decides" tabindex="-1">What declaration order decides <a class="header-anchor" href="#What-declaration-order-decides" aria-label="Permalink to &quot;What declaration order decides {#What-declaration-order-decides}&quot;">​</a></h2><p><strong>Order carries one meaning: draw order and overlay stacking.</strong> That is an authorial choice about what is drawn over what. It says nothing about reachability, and every module reaches every other module the same declaration named, wherever each one sits in the list.</p><p>The Core loads a declaration in three passes. It imports every module concurrently and runs no <code>setup</code>. It then runs each <code>setup</code>, in declaration order. It then replays the retained commands.</p><p>So one entry in the list is enough for a module that both <strong>feeds</strong> <code>ui</code>, with a float mount or a <code>positionOf</code> an anchor names, and <strong>extends</strong> it with a widget kind. There is no dependency declaration, because nothing needs one.</p><p>Three costs come with it.</p><ul><li><p><strong>A module&#39;s <code>setup</code> must not call a peer&#39;s functions.</strong> Every module&#39;s exports exist by then, but an accessor reading state the peer builds in its own <code>setup</code> answers <code>undefined</code>. The Core warns and names both modules, and still hands back the exports.</p></li><li><p><strong>The exports view is live</strong>, so unloading a provider drops it out from under a consumer that kept the id. Nothing on the wire unloads a single module, so this is recorded rather than guarded.</p></li><li><p><strong>A hanging import delays every module&#39;s <code>setup</code>.</strong> The same server serves the modules and sent the declaration, so there is no timeout and no retry.</p></li></ul><h2 id="The-one-hard-rule:-one-Cesium" tabindex="-1">The one hard rule: one Cesium <a class="header-anchor" href="#The-one-hard-rule:-one-Cesium" aria-label="Permalink to &quot;The one hard rule: one Cesium {#The-one-hard-rule:-one-Cesium}&quot;">​</a></h2><p>A module must not import Cesium at run time. It imports the types, which are erased at build, and reaches the live namespace through its context.</p><p>Two live copies of Cesium in one page is the dual-package hazard: a primitive built by one cannot be added to the other&#39;s scene, and a <code>Cartesian3</code> from one fails an <code>instanceof</code> in the other. The failures surface as blank geometry, nothing in the browser reports them, and a module author looking at an empty globe has no reason to suspect the loader. The scene, the clock, the pointer handler and the overlay arrive through the context for the same reason.</p><p>One route around the gate is legitimate. A bundle may mark a dependency <code>external</code> and alias it to <code>/modules/&lt;id&gt;/&lt;file&gt;.js</code>; the browser keys module instances by resolved URL, so both modules share one live instance. That import resolves in the browser, so the Core never sees it and cannot version-gate it. The route exists to stop a second copy of a third-party library a sibling module already bundles, and not to reach another module&#39;s exports: <code>ctx.modules</code> does that, and is gated.</p>',43)])])}const p=t(d,[["render",r]]);export{m as __pageData,p as default};
