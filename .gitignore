# The built viewer. `npm run build` writes it; a release artifact ships it.
lib/dist/
node_modules/
docs/node_modules/

# Julia resolver output — regenerated by Pkg.instantiate(), never committed
Manifest*.toml

# Documenter and Vitepress output
docs/build/
docs/final_site/
docs/src/public/viewer/
docs/src/public/recordings/
docs/src/examples/

# The packaged extension — a build output of `vsce package`, rebuilt on demand
extension/out/
*.vsix

*.log

# Working folders that never travel
.scratch/
.cdp/
.vscode/
.claude/
