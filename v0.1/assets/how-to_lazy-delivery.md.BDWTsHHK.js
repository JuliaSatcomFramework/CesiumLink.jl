import{_ as s,o as a,c as i,ao as t}from"./chunks/framework.sSVCmcJS.js";const c=JSON.parse('{"title":"Deliver a long mission a piece at a time","description":"","frontmatter":{},"headers":[],"relativePath":"how-to/lazy-delivery.md","filePath":"how-to/lazy-delivery.md","lastUpdated":null}'),n={name:"how-to/lazy-delivery.md"};function h(r,e,l,o,d,p){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="Deliver-a-long-mission-a-piece-at-a-time" tabindex="-1">Deliver a long mission a piece at a time <a class="header-anchor" href="#Deliver-a-long-mission-a-piece-at-a-time" aria-label="Permalink to &quot;Deliver a long mission a piece at a time {#Deliver-a-long-mission-a-piece-at-a-time}&quot;">​</a></h1><p>Your mission is fifty thousand keyframes. One window means you compute all of it before anything appears, hold all of it in the browser, and pay for the part nobody plays.</p><p>Declare the whole range and deliver a part of it. The viewer asks for the rest as playback approaches the end of what it holds.</p><h2 id="1.-Declare-the-whole-range,-deliver-a-part" tabindex="-1">1. Declare the whole range, deliver a part <a class="header-anchor" href="#1.-Declare-the-whole-range,-deliver-a-part" aria-label="Permalink to &quot;1. Declare the whole range, deliver a part {#1.-Declare-the-whole-range,-deliver-a-part}&quot;">​</a></h2><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">push_window</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(server, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">payload_for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            start_frame </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, count </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, total_frames </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 50_000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            dt_seconds </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, interval_seconds </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.15</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p><code>total_frames</code> is the mission. <code>count</code> is the buffer, what the viewer holds now. The clock, the ruler and scrubbing all work against the declared range, so the ruler spans the whole fifty thousand from the first window on.</p><p><strong>Prefer a small <code>count</code>.</strong> This package streams a mission a chunk at a time, and <code>count</code> is the chunk. A small window reaches the browser sooner, so the scene starts drawing earlier and the interaction stays responsive. It also bounds what the Core keeps: a <code>:replace</code> window is held entire, and an <code>append</code> is trimmed back to the span the buffer had before it, so the first window&#39;s <code>count</code> sets the memory footprint for the run.</p><p>Ten frames is a reasonable place to start. The Core keeps at least eight, so a smaller chunk saves nothing. Raise it when a measurement asks you to: a payload heavy enough that one chunk per interval does not keep up, or a link whose round trip is long enough that the buffer runs dry between asks.</p><p>Keep <code>total_frames</code> fixed for the run. A window that states a different one re-declares the range, which forces a <code>:replace</code> and drops the buffer — the opposite of what this page is for.</p><h2 id="2.-Register-the-listener" tabindex="-1">2. Register the listener <a class="header-anchor" href="#2.-Register-the-listener" aria-label="Permalink to &quot;2. Register the listener {#2.-Register-the-listener}&quot;">​</a></h2><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">on_event</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(server, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;core&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;need&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">do</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ev, _</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    push_window</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(server, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">payload_for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">start_frame, ev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">count);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                start_frame </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">start_frame, count </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">count, mode </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> :append</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                total_frames </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 50_000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, dt_seconds </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>The Core raises <code>core/need</code> as playback nears the end of the buffer. The event carries the <strong>1-based</strong> <code>ev.start_frame</code>, the <code>ev.count</code> asked for, and <code>ev.mode</code>.</p><p>Register it. Without a listener:</p><ul><li><p>a client that joins mid-session is sent only what the server retains, and</p></li><li><p>an append cannot stand on its own for that client, so it gets a scene that is missing whatever rode the window it never saw.</p></li></ul><p>Answer with more than you were asked for. The Core asks for the frames it wants, which can be two. Round the count up to a chunk you choose.</p><h2 id="3.-Answer-with-an-append" tabindex="-1">3. Answer with an append <a class="header-anchor" href="#3.-Answer-with-an-append" aria-label="Permalink to &quot;3. Answer with an append {#3.-Answer-with-an-append}&quot;">​</a></h2><p><code>mode = :append</code> extends the buffer the viewer holds. The window it extends keeps its identity, so a request raised against the earlier window is still valid.</p><div class="warning custom-block"><p class="custom-block-title">An append preserves the index space</p><p>Entity <code>i</code> is the same entity it was. An <a href="/CesiumLink.jl/v0.1/reference/primitives#CesiumLink.Primitives.Edges"><code>Edges</code></a> family joins two other families by index, and a pick reports an index, so a window that orders the entities differently makes both address something else. On screen it reads as every entity teleporting at the seam.</p></div><h2 id="4.-When-it-is-not-an-append" tabindex="-1">4. When it is not an append <a class="header-anchor" href="#4.-When-it-is-not-an-append" aria-label="Permalink to &quot;4. When it is not an append {#4.-When-it-is-not-an-append}&quot;">​</a></h2><p><strong>A gap is not an append.</strong> An append that starts past the end of the buffer leaves keyframes with no value, and there is nothing to interpolate across them. The viewer clears the buffer and refills it from the new window. Answer from the end of what you delivered.</p><p><strong>A control is a <code>:replace</code>.</strong> Push the replacement over the frames already delivered with <code>mode = :replace</code>. That window is free to renumber the entities, carry other families, or drop one.</p><div class="warning custom-block"><p class="custom-block-title">A <code>:replace</code> drops the batch the listener was building</p><p>The server compares the window identity before and after the listener chain runs, and sends nothing when it changed. Every command the chain put in <code>reply</code> is discarded: a tooltip, a float, a camera move and an overlay alike. A replace may renumber the entities, so an index resolved against the scene the event was raised on addresses something else now.</p><p>State what the new scene needs with a call of its own, such as <a href="/CesiumLink.jl/v0.1/reference/ui#CesiumLink.UI.declare_overlay"><code>declare_overlay</code></a>, rather than through <code>reply</code>.</p></div><h2 id="5.-What-a-reconnecting-client-gets" tabindex="-1">5. What a reconnecting client gets <a class="header-anchor" href="#5.-What-a-reconnecting-client-gets" aria-label="Permalink to &quot;5. What a reconnecting client gets {#5.-What-a-reconnecting-client-gets}&quot;">​</a></h2><p>A retained <code>:append</code> extends a <code>:replace</code> the new client never saw, so on its own it is a scene with holes in it.</p><p>The <code>core/need</code> listener answers that. When the last window was an append <strong>and the scene answers <code>core/need</code></strong>, a joining client is not sent the retained window: the scene is asked for a <code>:replace</code> over the same frames, and the client is sent that. A scene with no such listener replays the retained append as it stands.</p><p>A listener that always appends, whatever <code>ev.mode</code> says, does not leave that client blank. The server warns and falls back to the retained append, so the client draws the part of the scene that window covers.</p><p>So obey <code>ev.mode</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">push_window</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(server, payload;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            start_frame </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">start_frame, count </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">count, mode </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mode, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><h2 id="A-worked-example" tabindex="-1">A worked example <a class="header-anchor" href="#A-worked-example" aria-label="Permalink to &quot;A worked example {#A-worked-example}&quot;">​</a></h2><p><a href="./../examples/constellation">Constellation</a> delivers three hundred keyframes sixty at a time, with a control beside it. Its page states the declared range against the buffer, and holds the whole source.</p><h2 id="Next" tabindex="-1">Next <a class="header-anchor" href="#Next" aria-label="Permalink to &quot;Next {#Next}&quot;">​</a></h2><ul><li><p><a href="./../explanation/windows">Windows, keyframes and identity</a> — what a window is the unit of.</p></li><li><p><a href="./../reference/windows">Windows and scenes</a> — <code>push_window</code> and its keywords.</p></li><li><p><a href="./large-arrays">Send large arrays</a> — what one window carries cheaply.</p></li></ul>`,32)])])}const g=s(n,[["render",h]]);export{c as __pageData,g as default};
