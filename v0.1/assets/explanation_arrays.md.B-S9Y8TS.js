import{_ as a,o as t,c as s,ao as n}from"./chunks/framework.sSVCmcJS.js";const y=JSON.parse('{"title":"Arrays on the wire","description":"","frontmatter":{},"headers":[],"relativePath":"explanation/arrays.md","filePath":"explanation/arrays.md","lastUpdated":null}'),r={name:"explanation/arrays.md"};function o(i,e,h,d,l,c){return t(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="Arrays-on-the-wire" tabindex="-1">Arrays on the wire <a class="header-anchor" href="#Arrays-on-the-wire" aria-label="Permalink to &quot;Arrays on the wire {#Arrays-on-the-wire}&quot;">​</a></h1><p>A scene is mostly numbers. Positions, colours, link endpoints and raster grids are all arrays, and they dominate every message the server sends. How they travel is the wire&#39;s main design question (ADR-0016).</p><h2 id="Why-not-base64" tabindex="-1">Why not base64 <a class="header-anchor" href="#Why-not-base64" aria-label="Permalink to &quot;Why not base64 {#Why-not-base64}&quot;">​</a></h2><p>Base64 inside the JSON message is the obvious alternative. It adds <strong>a third to the wire</strong> whatever the machine, and its encode and decode together cost milliseconds per hundred kilobytes of array. Both costs scale linearly with the data: 20 MB of arrays adds 6.8 MB of wire, and its encode and decode reach the hundred-millisecond range. Three properties of this system make that worth avoiding.</p><ul><li><p><strong>The target is a remote host</strong>, and this WebSocket has no compression. Asset serving is gzipped; the socket is not.</p></li><li><p><strong>The <code>heatmap</code> module drapes baked RGBA.</strong> A 1024×512 raster is 2 MB per keyframe per layer.</p></li><li><p><strong>A notebook host encodes an ordinary push twice.</strong> A JSON frame that already holds base64 arrays goes through serialisation, base64 and JSON again — about 1.78× expansion over five passes.</p></li></ul><p>On a recorded satellite scene, the binary region carries the session in 23% fewer bytes than base64 carries the same scene.</p><h2 id="The-frame" tabindex="-1">The frame <a class="header-anchor" href="#The-frame" aria-label="Permalink to &quot;The frame {#The-frame}&quot;">​</a></h2><p>One WebSocket binary frame carries one message:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[u32 headerLen]   little-endian, at byte 0</span></span>
<span class="line"><span>[header]          headerLen bytes of UTF-8 JSON: the whole message</span></span>
<span class="line"><span>[pad]             zero bytes, up to the next multiple of 8</span></span>
<span class="line"><span>[region]          the array bytes the header points into</span></span></code></pre></div><p>The header is the message, and the <strong>region</strong> is the array bytes behind it. A message with no arrays has an empty region, and the framing is identical either way, so there is one code path rather than two. An encoded array in the header is a self-describing triple:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;$wire&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;f32&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;shape&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">264</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;off&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4096</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p><strong>The offset counts from the start of the region, never from the start of the frame.</strong> Some hosts hand the region over on their own; a notebook host carries the whole packed frame as one array. A frame-relative offset would need a fixup on that transport and not on the other, where region-relative offsets let both receive one buffer and split it at the <code>u32</code>.</p><p><strong>The offset is always a multiple of 8, whatever the dtype.</strong> One rule instead of a table per dtype. The waste is at most 7 bytes per array — 147 bytes across a recorded 21-array window — and 8 is what a <code>Float64Array</code> view needs anyway.</p><h2 id="Why-shape-is-mandatory-and-the-length-is-not-carried" tabindex="-1">Why <code>shape</code> is mandatory and the length is not carried <a class="header-anchor" href="#Why-shape-is-mandatory-and-the-length-is-not-carried" aria-label="Permalink to &quot;Why \`shape\` is mandatory and the length is not carried {#Why-shape-is-mandatory-and-the-length-is-not-carried}&quot;">​</a></h2><p>The length is derived: <code>prod(shape) × bytesPerElement</code>. A length field beside the shape is redundant with <code>shape</code> and the dtype tag, and a redundant field is one more thing to keep in step. What stands in its place is a bounds check, <code>off + length ≤ region.byteLength</code>, refusing the frame when it does not hold: that one line stands between a malformed frame and a view over bytes that never arrived.</p><p>A separate <code>buffers</code> table with index references was declined for the same reason at one remove. It adds indirection to describe what one offset already says.</p><h2 id="Every-array-is-a-view" tabindex="-1">Every array is a view <a class="header-anchor" href="#Every-array-is-a-view" aria-label="Permalink to &quot;Every array is a view {#Every-array-is-a-view}&quot;">​</a></h2><p>The browser reads each array as a <strong>view</strong> into the region. <code>data.buffer</code> is the whole received frame, and <code>data.byteOffset</code> is where that array sits in it. The bytes are never copied.</p><p>Copying costs on the order of a millisecond per megabyte, so tens of milliseconds on a 20 MB window, and that is most of what this design saves. It costs a lifetime coupling: a module that keeps a slice past its window keeps the whole frame alive with it. The coupling is bounded, because a module&#39;s per-window store is keyed on the window object and <code>primitives</code> holds two windows across a seam. A module that wants a detached copy makes one explicitly.</p><h2 id="The-row-major-label-reversal" tabindex="-1">The row-major label reversal <a class="header-anchor" href="#The-row-major-label-reversal" aria-label="Permalink to &quot;The row-major label reversal {#The-row-major-label-reversal}&quot;">​</a></h2><p><code>shape</code> is row-major, with the last dimension varying fastest. Julia is column-major, and it <strong>reverses the label</strong> at its own boundary. The label changes and no bytes move. Readers often mistake the reversal for a transpose.</p><p>Consider a Julia array of size <code>(3, N)</code>, three components for each of <code>N</code> entities. Its flat byte order is component-major: the three components of entity 1, then the three of entity 2, and so on. The wire states <code>shape: [N, 3]</code>, and a browser reading that shape row-major walks the same bytes in the same order — <code>N</code> rows of 3. Both sides land on their own idiom, and a <code>Vector{ECEF}</code> travels correctly as a reinterpretation with no codec change at all.</p><p>The dtype table is small on purpose. Five types travel — <code>f32</code>, <code>f64</code>, <code>u8</code>, <code>u32</code>, <code>i32</code> — and any other numeric element type is converted to the one that carries it without loss, or refused. A value the target dtype cannot hold is an error rather than a wrap-around, so the bytes on the wire always mean what the sender held. There is no 64-bit integer dtype: an <code>Int64</code> past <code>Int32</code> travels only as <code>Float64</code>, exact up to 2^53. Anything that is not a number becomes an ordinary JSON list, which is what makes a list of label strings work.</p><h2 id="Why-there-are-no-requests" tabindex="-1">Why there are no requests <a class="header-anchor" href="#Why-there-are-no-requests" aria-label="Permalink to &quot;Why there are no requests {#Why-there-are-no-requests}&quot;">​</a></h2><p>The header is JSON-RPC-2.0-shaped without the <code>jsonrpc</code> field, and a message with an <code>id</code> would be a request expecting a result. <strong>The protocol uses none.</strong> Everything is a notification in one direction or the other.</p><p>No answer here fits the request shape. Every answer is either a command batch or a window, and both may arrive later than the thing that prompted them, more than once, or not at all. A pointer event may be answered by nothing, because no listener had anything to say; a control may be answered by a window and then by a re-declaration of the overlay.</p><p>Correlation lives in the payload rather than in the transport. An event carries a sequence number, and the command batch that answers it echoes that number, so a module that cares about staleness has what it needs and the transport stays a one-directional pipe.</p><h2 id="Only-the-downward-half-exists" tabindex="-1">Only the downward half exists <a class="header-anchor" href="#Only-the-downward-half-exists" aria-label="Permalink to &quot;Only the downward half exists {#Only-the-downward-half-exists}&quot;">​</a></h2><p>The frame layout is defined for both directions, and <strong>only the downward array encoder is built</strong>. Nothing puts an array in an upward payload today: pointer events, buffer requests, control input and the ellipsoid confirmation are all scalars. A browser sends an empty region and refuses a typed array in an event payload, naming the conversion that would fix it. The server is authoritative, so bulk data that flows upward inverts the model. Building the upward half later means writing an encoder against a contract that already describes it, and changing nothing else.</p><h2 id="What-was-declined,-and-why" tabindex="-1">What was declined, and why <a class="header-anchor" href="#What-was-declined,-and-why" aria-label="Permalink to &quot;What was declined, and why {#What-was-declined,-and-why}&quot;">​</a></h2><ul><li><p><strong>Two live encodings, negotiated or switched.</strong> The codec is the whole payload contract, so a disagreement between two encodings is silent and total. A frame-decoding tool and readable recording headers buy the debuggability back instead.</p></li><li><p><strong>A transport that substitutes bytes into payload internals</strong>, so that the Core sees no change. That logic would be written once per transport instead of once in the codec. A transport splits the frame and passes both parts up; it never reads a payload.</p></li><li><p><strong>MsgPack.</strong> A dependency on each side, plus hand-written extension handlers, to buy what a framed blob region gives for nothing.</p></li><li><p><strong>A magic number or a per-frame version byte.</strong> Redundant with a handshake that has to exist, and a viewer that does not know the format will not look for the magic number either. The handshake is the only place the disagreement can be named, which is why a version mismatch closes the socket with a reason.</p></li></ul><h2 id="One-consequence-worth-knowing" tabindex="-1">One consequence worth knowing <a class="header-anchor" href="#One-consequence-worth-knowing" aria-label="Permalink to &quot;One consequence worth knowing {#One-consequence-worth-knowing}&quot;">​</a></h2><p><strong>Julia only builds frames and the browser only reads them.</strong> Neither side can round-trip against itself, so nothing in either test suite would catch the two drifting apart. A golden frame on disk is the pin: a Julia script writes it, the Julia suite asserts the bytes, and a browser-side test decodes it. Its payload is chosen so that the offsets need a pad of 5, then 7, then 4, because a frame of same-dtype arrays lands 8-aligned throughout and exercises no padding at all.</p><p>The normative statement of the frame and the dtype table is in the <a href="./../reference/wire/protocol">wire protocol</a>; the Julia side of the codec is in the <a href="./../reference/codec">codec reference</a>, and <a href="./../how-to/large-arrays">Send large arrays</a> is the practical guide.</p>`,34)])])}const w=a(r,[["render",o]]);export{y as __pageData,w as default};
