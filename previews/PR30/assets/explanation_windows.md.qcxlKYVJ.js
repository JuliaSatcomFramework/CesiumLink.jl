import{_ as a,o as t,c as n,ao as o}from"./chunks/framework.B6MdFEBY.js";const w=JSON.parse('{"title":"Windows, keyframes and identity","description":"","frontmatter":{},"headers":[],"relativePath":"explanation/windows.md","filePath":"explanation/windows.md","lastUpdated":null}'),s={name:"explanation/windows.md"};function r(i,e,d,h,l,c){return t(),n("div",null,[...e[0]||(e[0]=[o('<h1 id="Windows,-keyframes-and-identity" tabindex="-1">Windows, keyframes and identity <a class="header-anchor" href="#Windows,-keyframes-and-identity" aria-label="Permalink to &quot;Windows, keyframes and identity {#Windows,-keyframes-and-identity}&quot;">​</a></h1><p>Time-varying scene data reaches the browser in one shape: a <strong>window</strong>, a contiguous run of keyframes carrying every module&#39;s payload for those frames. There is no second carrier. A static scene is a window of one frame.</p><h2 id="The-declared-range-and-the-delivered-buffer" tabindex="-1">The declared range and the delivered buffer <a class="header-anchor" href="#The-declared-range-and-the-delivered-buffer" aria-label="Permalink to &quot;The declared range and the delivered buffer {#The-declared-range-and-the-delivered-buffer}&quot;">​</a></h2><p>Two ranges exist at once. Do not confuse them.</p><ul><li><p>The <strong>declared range</strong> is the whole run: an epoch, a keyframe step, and a total count. Every window message states it. The range is finite and known even when almost none of it is delivered, and the clock, the timeline ruler and scrubbing work against it.</p></li><li><p>The <strong>delivered buffer</strong> is the subset of keyframes the viewer holds. Interpolation works against nothing else.</p></li></ul><p>The separation lets a long run start immediately: a 240-keyframe mission declares 240 frames in its first window, delivers two, and the ruler already spans the right dates. A range derived from the frames delivered so far makes the ruler grow under the user&#39;s hand. An open-ended stream with no declared end takes the meaning out of the ruler and out of scrubbing and makes a loop impossible, for a flexibility the domain does not need (ADR-0008).</p><p>An instant the buffer does not cover is not an error. The clock stops there, the last delivered frame stays on screen, and the Core asks for the window that covers it, so starvation shows as a pause. The hold clears the clock&#39;s <code>canAnimate</code> and never its <code>shouldAnimate</code>, so a pause the user pressed during a hold survives the release.</p><p>Dragging the ruler writes <code>shouldAnimate</code> false, exactly as the play button does, because moving the clock by hand is a deliberate pause. The Core still asks for the window the scrub lands outside of, and still installs it, but the clock stays where the user put it until the user presses play.</p><h2 id="What-an-index-means" tabindex="-1">What an index means <a class="header-anchor" href="#What-an-index-means" aria-label="Permalink to &quot;What an index means {#What-an-index-means}&quot;">​</a></h2><p>Inside one window, index <code>i</code> addresses the same object in every frame of that window. That is the whole identity guarantee, and it is sufficient, because interpolation only brackets two frames of the same window.</p><p>Per-tick interpolation blends the position at index <code>i</code> in one keyframe into the position at index <code>i</code> in the next, so an index that means different objects in consecutive frames teleports every entity on every step. An index space fixed for the whole session is the other extreme: the payload can never shrink, and a filter becomes a visibility mask, which cannot express a filter that changes derived values.</p><p>One consequence is structural: <strong>every module&#39;s payload for the same frames arrives in one message and installs together</strong>. Separate delivery leaves a span of one frame in which the scene and an overlay drawn over it disagree about what index <code>i</code> addresses.</p><h2 id="The-two-push-kinds" tabindex="-1">The two push kinds <a class="header-anchor" href="#The-two-push-kinds" aria-label="Permalink to &quot;The two push kinds {#The-two-push-kinds}&quot;">​</a></h2><p>Between windows, <em>why</em> the window was pushed sets the rule.</p><p>A <strong>streaming advance</strong> tops up the buffer as playback approaches its end. The user asked for nothing, so nothing may visibly move at the seam, and the advance <strong>must</strong> preserve the previous window&#39;s index space. It travels as <code>mode: append</code> and repeats the window identity it extends.</p><p>A <strong>control re-push</strong> replaces the buffer in answer to an event. The user asked for a visible change, so the window <strong>may</strong> re-index freely. It travels as <code>mode: replace</code> and mints a new window identity.</p><p>The buffer has exactly two operations: extend, and clear and refill. They map one to one onto the two push kinds, which makes the identity rule structural. There is no third mode and no way to express one.</p><p>The Core checks the label rather than trusting it. An <code>append</code> extends the buffer only if it continues it at either end: its first frame is one past the last frame held, or its last frame is one before the first. A clock running backwards consumes the buffer downwards and gets windows that extend it that way. An <code>append</code> that leaves a gap or overlaps cannot be interpolated across, so it clears the buffer and re-bases on itself, whatever its mode field says.</p><p>The buffer is bounded, and the bound travels with the clock: an extension past the bound drops frames from the end playback is moving away from, so the frames held follow the clock whichever way it runs. Only an extension is bounded, so a server that delivers a whole run in one window gives none of it up. A dropped frame is asked for again if the clock returns to it.</p><h2 id="Why-interpolation-never-crosses-a-window" tabindex="-1">Why interpolation never crosses a window <a class="header-anchor" href="#Why-interpolation-never-crosses-a-window" aria-label="Permalink to &quot;Why interpolation never crosses a window {#Why-interpolation-never-crosses-a-window}&quot;">​</a></h2><p>Two frames from different windows may carry different index spaces, so blending between them blends two different sets of objects, and the Core never offers such a pair. A module reads its arrays through the <strong>placement</strong> of an absolute keyframe: which window carries it, and the offset within that window. Two placements from one window are safe to blend; two from different windows are two separate draws.</p><p>A module&#39;s store of window payloads is keyed on the <strong>window object</strong>, never on the start frame, because a <code>replace</code> gives the same absolute indices to a different window and a number-keyed map then addresses the wrong payload across the seam. Keying on the object also spares the store a retention bound: a value becomes unreachable exactly when the Core stops naming its window.</p><h2 id="Keyframes-and-base-rank" tabindex="-1">Keyframes and base rank <a class="header-anchor" href="#Keyframes-and-base-rank" aria-label="Permalink to &quot;Keyframes and base rank {#Keyframes-and-base-rank}&quot;">​</a></h2><p>Inside a payload, a value may vary per keyframe or stand still for the whole window. The rule that decides which is the <strong>base rank</strong>, and the Core owns it (ADR-0014).</p><p>A module knows the rank of the form it expects: 1 for one value per entity, 3 for an <code>[H, W, 4]</code> raster. An array at or below that rank holds one value for the whole window, and every keyframe reads all of it. An array one rank above it carries a leading keyframe axis, and keyframe <code>k</code> reads the contiguous block at <code>k × block</code>. Any higher rank is an error.</p><p>The rule sits in the Core because it says nothing about what the values mean, and four modules that decided it separately reached four conventions. The Core states it once, as <code>blockAt</code>, and checks it: an array whose leading axis disagrees with the window it rode in on throws instead of reading a short block.</p><p>Meaning stays with the module. The components an entity takes, the four RGBA bytes of a texel, and a stride of zero that covers a whole family are entity semantics, and a Core that knew any of them would have to model an entity. Two designs were declined for that reason. A full slicer in the Core would absorb the raster contract and the notion of a family. A <code>payloadAt(index)</code> accessor answers three of the four modules and makes the Core retain payloads nobody reads: <code>primitives</code> keeps derived state rather than the raw payload, and reads two placements at once across a seam.</p><h2 id="Where-the-requests-come-from" tabindex="-1">Where the requests come from <a class="header-anchor" href="#Where-the-requests-come-from" aria-label="Permalink to &quot;Where the requests come from {#Where-the-requests-come-from}&quot;">​</a></h2><p>No module asks for a window. The Core watches the clock against the coverage it holds, and asks when playback nears the edge it is heading for, or when the clock is scrubbed past coverage. The request names an absolute start index and a frame count: one frame where the window continues the buffer at either end, and two where it lands somewhere new, because interpolation needs a neighbour to blend toward.</p><p>The count travels with the request, so the server needs no memory of what it already sent. Its answer is a pure function of the request and its own data, which lets a recorded session replay and a late client catch up.</p><p>The Julia side is in the <a href="./../reference/windows">windows reference</a>, and the wire fields are in the <a href="./../reference/wire/protocol">protocol</a>. To answer those requests in a program, read <a href="./../how-to/lazy-delivery">Deliver a long mission a piece at a time</a>.</p>',31)])])}const p=a(s,[["render",r]]);export{w as __pageData,p as default};
