import{_ as t,o as a,c as s,ao as n}from"./chunks/framework.DSlTPKvO.js";const u=JSON.parse('{"title":"Why the server decides","description":"","frontmatter":{},"headers":[],"relativePath":"explanation/server-authoritative.md","filePath":"explanation/server-authoritative.md","lastUpdated":null}'),r={name:"explanation/server-authoritative.md"};function i(o,e,h,l,d,c){return a(),s("div",null,[...e[0]||(e[0]=[n('<h1 id="Why-the-server-decides" tabindex="-1">Why the server decides <a class="header-anchor" href="#Why-the-server-decides" aria-label="Permalink to &quot;Why the server decides {#Why-the-server-decides}&quot;">​</a></h1><p>Operating a control in the viewer changes nothing in the viewer. The widget reports that the user operated it, and the server answers with a replacement window whose contents already reflect the decision. Filtering, selection and derived visibility live in the Julia half, and the viewer renders what it is given (ADR-0007).</p><h2 id="The-argument-that-settled-it" tabindex="-1">The argument that settled it <a class="header-anchor" href="#The-argument-that-settled-it" aria-label="Permalink to &quot;The argument that settled it {#The-argument-that-settled-it}&quot;">​</a></h2><p>This package is for Julia users, and it assumes the computation is Julia&#39;s. It rests on two premises: the Julia side produces a frame faster than playback consumes it, and the socket delivers that frame faster still. Neither the computation nor the wire is the bottleneck. So the scene is decided where the data already is, and the viewer is left with one job.</p><p>A viewer that decides anything must first be sent what it decides with. That costs wire bandwidth and page memory, and much of it is never drawn: a page that filters holds everything the user could ask to see, and not what the user asked to see.</p><p>One authority also keeps the contract small. There is no table of which decisions belong to which half, no viewer state to reconcile after a window is replaced, and one place to read when the scene shows something unexpected.</p><p>The alternative is a viewer that filters locally: ship a classification per entity, let the user tick a box, and hide what does not match. It needs a tag encoding, a rule for composing two filters, and a derived-visibility pass in the viewer. It also fails outright on one case. Restricting a constellation to the satellites over a region makes cells <strong>unserved</strong>, because the traffic they carried had nowhere else to go. No visibility mask expresses that: the tooltip number, the cell colour and the legend total all change, and only the simulation produces those values.</p><p>Draw calls also scale with what is <strong>sent</strong>, not with what is shown. A hidden polyline still occupies its vertex buffer, its draw command and its vertex transform, and only its fragments are skipped. Filtering server-side removes the work; filtering client-side moves it.</p><h2 id="The-widget-always-shows-the-declared-value" tabindex="-1">The widget always shows the declared value <a class="header-anchor" href="#The-widget-always-shows-the-declared-value" aria-label="Permalink to &quot;The widget always shows the declared value {#The-widget-always-shows-the-declared-value}&quot;">​</a></h2><p>A control carries the value the server declared, and the user&#39;s input does not change what the widget shows: the declaration moves it. So a control the server refuses to act on snaps back, and a toggle that stays put tells the user the truth about what the scene shows.</p><p>The rule has one exception. Where the user dragged an adjustable float, a later declaration seeds a box when it is created and moves no box already on screen. A rect is not scene state, because nothing is filtered by where a box sits, so a viewer showing one position while the server believes another misleads nobody. The exception stops a declaration already in flight from snapping the box back, and that race is a full round trip wide (ADR-0013).</p><h2 id="The-clock-is-not-held-for-the-answer" tabindex="-1">The clock is not held for the answer <a class="header-anchor" href="#The-clock-is-not-held-for-the-answer" aria-label="Permalink to &quot;The clock is not held for the answer {#The-clock-is-not-held-for-the-answer}&quot;">​</a></h2><p>This is about the round trip that answers a control event, and about nothing else the user does to the clock. Playback runs on through that trip, and the scene changes when the replacement window installs and re-bases the clock on what it delivers. The superseded state stays on screen for the length of the trip, a few milliseconds on a local host. An earlier design froze the clock on a control event and resumed on arrival.</p><p>The answer never writes <code>shouldAnimate</code>, which is the flag that says whether the user wants playback. So a control operated on a paused scene leaves it paused. Two other things do stop the clock, and neither of them is this round trip:</p><ul><li><p>A <strong>scrub</strong> stops it, because the user moved the clock by hand. See <a href="./windows">Windows, keyframes and identity</a>.</p></li><li><p>A <strong>buffer that does not reach the current instant</strong> holds it. The hold lifts as soon as a window covers that instant.</p></li></ul><h2 id="The-listener-chain" tabindex="-1">The listener chain <a class="header-anchor" href="#The-listener-chain" aria-label="Permalink to &quot;The listener chain {#The-listener-chain}&quot;">​</a></h2><p>The Julia side keeps a registry of listeners keyed by <code>(module, topic)</code>. A pointer event is <code>(&quot;core&quot;, &quot;pointer&quot;)</code>, with no special case for it. An arriving event runs every listener registered for that pair, in registration order, over one shared reply.</p><ul><li><p><strong>A listener may halt the chain.</strong> What the listeners ahead of it contributed is still sent, so halting withholds the listeners behind it and never the answer.</p></li><li><p><strong>A listener that throws is isolated.</strong> It loses its own contribution, the warning carries the backtrace, and the listeners behind it still run.</p></li><li><p><strong>The chain couples cheap and expensive listeners.</strong> The batch is assembled after the whole chain runs, so one slow listener delays every other contribution to that event. A hover fires on every rendered pointer move, so a hover listener that computes something heavy delays the answer to each move, and the tooltip falls behind the pointer. The chain enforces no budget.</p></li></ul><h2 id="One-event,-one-message" tabindex="-1">One event, one message <a class="header-anchor" href="#One-event,-one-message" aria-label="Permalink to &quot;One event, one message {#One-event,-one-message}&quot;">​</a></h2><p>Everything the chain contributed travels as a single <strong>command batch</strong>, and the viewer applies it in the order the chain built it. One reply per listener was rejected: a hover over an entity that three listeners care about would become three messages applied at three instants, and the tearing shows in the tooltip and in the overlay.</p><p>A batch that answers an event echoes that event&#39;s sequence number, and the Core applies every batch whatever the number says. The <strong>receiving module</strong> drops a stale reply, because a late answer to a click is often still worth having, a late answer to a hover usually is not, and only the module knows which it has.</p><p>One case is handled in Julia. A listener chain that pushes a <code>replace</code> window has its whole batch dropped, tooltip included: a replace may renumber entities, so the indices the batch carries describe a scene that no longer exists, and the fresh state is already on its way.</p><h2 id="Where-the-subscription-comes-from" tabindex="-1">Where the subscription comes from <a class="header-anchor" href="#Where-the-subscription-comes-from" aria-label="Permalink to &quot;Where the subscription comes from {#Where-the-subscription-comes-from}&quot;">​</a></h2><p>The viewer forwards a pointer event upward only when the server has a listener for it, and the subscription the server pushes is <strong>computed</strong> from the listeners currently registered (ADR-0010).</p><p>Two alternatives lost. Forwarding everything makes a hover cost a round trip per pointer move in a session with no hover listener, to be told that nothing happened. A subscription the author declares beside the listeners is two statements of one fact, and its failure mode is silent: the event never arrives, and nothing on either side names the subscription as the reason.</p><p>Derivation also composes. Two independent extensions each register the listeners they need, and the union of their interests is declared without either knowing about the other. A listener that leaves some modifiers unmentioned is expanded into every modifier set consistent with what it did name, because a subscription entry can only name a set exactly.</p><p>The globe raycast under the cursor is done only when some registered listener asked for the coordinate, so a session that never asks never pays for it.</p><h2 id="Retained-state,-and-what-a-reconnecting-client-gets" tabindex="-1">Retained state, and what a reconnecting client gets <a class="header-anchor" href="#Retained-state,-and-what-a-reconnecting-client-gets" aria-label="Permalink to &quot;Retained state, and what a reconnecting client gets {#Retained-state,-and-what-a-reconnecting-client-gets}&quot;">​</a></h2><p>The server holds three things and replays them to a client on <code>ready</code>: the module set, the latest command per <code>(module, topic)</code>, and the standing window. So a client that reconnects comes back to the same scene, with its overlay showing the values the scene was actually filtered with. A reloaded page and a reopened VSCode panel both take that path.</p><p>Retention holds <strong>one</strong> message per <code>(module, topic)</code>, which is why every declaration states its whole set rather than a patch. A stream of partial patches would replay only its last frame, and the client would come back to a fragment of an overlay. The overlay list, the subscription, the furniture set and the float set are all whole statements for this reason. Event history is never replayed: a declaration-shaped topic restores itself, and an event-shaped topic like a tooltip is overwritten by the next pointer move.</p><p>The window has one wrinkle. <strong>Only a replacing window is replayable.</strong> An <code>append</code> extends a window the joining client never received, and may omit anything that window established. So on an append the server asks the scene for a replacement covering the same frames and broadcasts that instead; the clients already watching are re-based on it and ask for what they are then missing, at one round trip per join. Where no window producer is registered, the retained append is sent as it stands. A client that receives nothing at all raises no request of its own, and has no way back from silence.</p><h2 id="The-three-depths-of-an-answer" tabindex="-1">The three depths of an answer <a class="header-anchor" href="#The-three-depths-of-an-answer" aria-label="Permalink to &quot;The three depths of an answer {#The-three-depths-of-an-answer}&quot;">​</a></h2><p>A server can answer a control event at three depths, in ascending cost.</p><ul><li><p><strong>Re-extract.</strong> The simulation result stands, and only which entities are emitted changes. Hiding the feeder links is this.</p></li><li><p><strong>Re-derive.</strong> The allocations stand, but the aggregation changes, so derived values genuinely differ. Counting only the satellites over Europe is this, and it is what makes cells unserved.</p></li><li><p><strong>Re-simulate.</strong> The run itself is redone.</p></li></ul><p>The click path is defined to reach re-derive and stop, which bounds its latency to array work over results the server already holds. It costs memory: the server must retain each buffered frame&#39;s simulation output, so buffer depth is bounded by memory as well as by production rate. Re-simulation is slower again, and it needs a control of its own, so nobody starts a run by ticking a box.</p><p>The Julia API is in the <a href="./../reference/events">events reference</a>, and the message shapes are in the <a href="./../reference/wire/protocol">protocol</a>.</p>',36)])])}const w=t(r,[["render",i]]);export{u as __pageData,w as default};
