import{_ as a,o as t,c as s,ao as o}from"./chunks/framework.CraasqCu.js";const u=JSON.parse('{"title":"Take a picture of the globe","description":"","frontmatter":{},"headers":[],"relativePath":"how-to/capture.md","filePath":"how-to/capture.md","lastUpdated":null}'),i={name:"how-to/capture.md"};function r(n,e,h,l,c,p){return t(),s("div",null,[...e[0]||(e[0]=[o('<h1 id="Take-a-picture-of-the-globe" tabindex="-1">Take a picture of the globe <a class="header-anchor" href="#Take-a-picture-of-the-globe" aria-label="Permalink to &quot;Take a picture of the globe {#Take-a-picture-of-the-globe}&quot;">​</a></h1><p>You want a still of the scene: a figure for a paper, a slide, a bug report. A <strong>canvas capture</strong> is one PNG of the viewer&#39;s canvas. Julia asks for one and writes the file, and a furniture button copies one to the clipboard.</p><h2 id="What-a-capture-holds" tabindex="-1">What a capture holds <a class="header-anchor" href="#What-a-capture-holds" aria-label="Permalink to &quot;What a capture holds {#What-a-capture-holds}&quot;">​</a></h2><p>The canvas, and nothing else. The <strong>furniture</strong>, the overlay <strong>controls</strong> and the <strong>floats</strong> are HTML boxes above the canvas, so they are never in a capture.</p><p>This is a property of where they sit, not a setting. The timeline was never drawn on the canvas, so there is nothing to take off it. A capture shows the globe with every entity the modules drew on it.</p><h2 id="Ask-from-Julia" tabindex="-1">Ask from Julia <a class="header-anchor" href="#Ask-from-Julia" aria-label="Permalink to &quot;Ask from Julia {#Ask-from-Julia}&quot;">​</a></h2><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">capture_canvas</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(server, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;fig.png&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; scale </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>The call blocks. It writes the file and returns the path.</p><div class="warning custom-block"><p class="custom-block-title">Warning</p><p>Do not call this from an event listener. The server sends a listener chain&#39;s command batch only after the chain completes, so a capture inside a listener waits for an answer it holds up itself.</p></div><p>Every connected viewer answers, because the server broadcasts and a client has no id. A picture wins over a refusal, whoever answered first.</p><p>When two viewers both draw, both answers are valid pictures and the server keeps whichever arrived first. The camera belongs to the user, so the two can show different views, and nothing says which of them you get. Aim this at a session with one browser open.</p><p>The call throws in four cases: no viewer receives the request, none answers inside <code>timeout</code> seconds, every viewer refuses, or one viewer refuses and another never answers. A refusal carries the viewer&#39;s own reason.</p><p>The fourth case waits the whole <code>timeout</code> out, then reports the refusal. A picture beats a refusal, so the call waits for the silent viewer as long as it may still draw one.</p><h2 id="Ask-for-more-pixels" tabindex="-1">Ask for more pixels <a class="header-anchor" href="#Ask-for-more-pixels" aria-label="Permalink to &quot;Ask for more pixels {#Ask-for-more-pixels}&quot;">​</a></h2><p><code>scale</code> multiplies the drawing buffer. A capture at scale 2 of a 1400x800 canvas is 2800x1600 pixels.</p><p>More pixels is not more view. The framing is exactly what you see: scale sharpens the same picture, it does not widen it. To take in more of the globe, move the camera or make the window bigger.</p><p>The viewer refuses a scale its GPU cannot draw, and the reason names the size the call asked for. Nothing comes back blank.</p><h2 id="Put-the-button-on-screen" tabindex="-1">Put the button on screen <a class="header-anchor" href="#Put-the-button-on-screen" aria-label="Permalink to &quot;Put the button on screen {#Put-the-button-on-screen}&quot;">​</a></h2><p>The <code>canvas_capture</code> furniture item is off by default, so a scene asks for it:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">declare_furniture</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(server; canvas_capture </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><ul><li><p>A left click copies a capture to the clipboard at scale 1.</p></li><li><p>A right click opens a popup that holds a filename, a scale, a <code>Copy</code> button and a <code>Download</code> button.</p></li><li><p>A long press does what a right click does, for a touch screen.</p></li></ul><p>The viewer keeps none of it. There is no stored setting, and the popup starts from its defaults every time.</p><h2 id="When-the-clipboard-does-not-work" tabindex="-1">When the clipboard does not work <a class="header-anchor" href="#When-the-clipboard-does-not-work" aria-label="Permalink to &quot;When the clipboard does not work {#When-the-clipboard-does-not-work}&quot;">​</a></h2><p>The browser gives the clipboard only to a secure context. So <code>localhost</code> and HTTPS reach it, and plain HTTP to another machine does not. A left click opens the popup instead when the clipboard is out of reach, and the popup shows the <code>Copy</code> button disabled with the reason.</p><p>The clipboard also needs a real click. Julia therefore cannot copy to the clipboard, and <code>capture_canvas</code> writes a file instead.</p><h2 id="What-a-capture-is-not" tabindex="-1">What a capture is not <a class="header-anchor" href="#What-a-capture-is-not" aria-label="Permalink to &quot;What a capture is not {#What-a-capture-is-not}&quot;">​</a></h2><p>A capture is a request, and not scene state. The server retains none of it and a <strong>recording</strong> holds none of it: a recording drives a viewer with no server behind it, which has nothing to answer to.</p><p>See <a href="/CesiumLink.jl/dev/reference/furniture#CesiumLink.capture_canvas"><code>capture_canvas</code></a> and <a href="/CesiumLink.jl/dev/reference/furniture#CesiumLink.declare_furniture"><code>declare_furniture</code></a>.</p>',28)])])}const f=a(i,[["render",r]]);export{u as __pageData,f as default};
